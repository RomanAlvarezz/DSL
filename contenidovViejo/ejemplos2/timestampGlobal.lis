timestamp.database("v1");

transaction{
  insert.users({name: "Test1", age: 50});
  insert.users({name: "Test2", age: 60})
};

find.users()
  .preview();

rollback.database("v1");

find.users()
  .preview()
