createView("mayores",
  find.users()
    .filter(!(age < 18 + 2))
    .preview()
);

useView("mayores");

transaction{
  insert.users({name: "Demi", age: 22});
  insert.users({name: "Tedej", age: 27})
};

useView("mayores")
