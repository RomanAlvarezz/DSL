insert.animales({name:"Hienarda", age:76, tipo:"terrestre"});

find.animales()
  .filter(exists(tipo) && age > 3) 
  .groupby(tipo)
  .sum("totalEdad", age)
  .avg("promEdad", age)
  .having(promEdad > 3)
  .sort({promEdad: desc})
  .preview()
