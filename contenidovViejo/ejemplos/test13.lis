
createView("mayoresActivos",
  find.users()
    .filter(!(age < 18))
    .groupby(age)
      .count("total", edad)
      .having(total > 1)
    .preview()
);



useView("mayoresActivos")
  .filter(age > 30)
  .sort({age: desc})
  .limit(5)
  .save("hola2.json");

useView("mayoresActivos");


